# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts

# builds
build/
dist/

# env & secrets — never commit these
.env
.env.local
.env*.local

# database
*.sqlite
*.db

# uploads (local file storage in dev) — anchored to repo root so this
# doesn't also match src/app/api/v1/uploads/ (a real route, not output)
/uploads/
/public/uploads/

# logs & misc
*.log
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# prisma generated client is derived, not source
src/generated/
*.tsbuildinfo

.vercel
.env*.local
